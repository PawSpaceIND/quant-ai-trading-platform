# PR #126 engineering checkpoint and continuation boundary

This checkpoint consolidates the institutional paper-platform work and the four
risk-acceptance repairs. It is a source-integration milestone, not a deployment,
trading authorization, complete product acceptance or profitability claim.

## Integrated sources

- PR #135 risk repair: `4b85cf7daff7bfebcab65590c5bc1f10f8b561fa`.
- Merge of #135 into #126: `aa40c2c8dfa00087fc157512700033b3062e7b9b`.
- Main refresh, including #134: `58a3d72a6fafbd17df97bccb78943a30be06df48`.
- Combined pre-documentation head: `0b19110a4352357809eee74539f571b8162d9b38`.

The #135 merge tree matched the certified risk head exactly. The main refresh had
no conflicts and preserved all five incoming #134 files byte-for-byte. The original
17-case risk-acceptance file remains byte-for-byte unchanged. Final publication and
post-merge commit IDs, job IDs and completed results are recorded in PR #126.
Earlier entries in CLOSURE_CHECKPOINT_20260917.md remain historical evidence.

## What this checkpoint includes

The existing contract-bound order/OMS path; decision and recovery identity checks;
reviewed paper-order recovery; independent protective-exit accounting; complete
selected OMS/accounting/program/AI recovery bundles; authenticated read-only OMS
inspection; research publication; AI knowledge/training/candidate controls; and
native Mac/Linux deployment-script compatibility remain integrated.

The four risk repairs cover after-cost Kelly arithmetic, the whole-parent modeled
loss allowance across execution slices, projected factor-book consistency, and
finite/nonnegative strategy-exposure input validation. Covered exits remain exempt
from entry-only evidence requirements. These checks remain active in CI.

## Separate release gates — not closed by a source merge

The full institutional coordinator is not yet the normal daemon execution path.
The legacy cash mode remains the default; bound mode still needs explicit reviewed
configuration. No account is migrated, recovered, restarted or activated by Git.
No live broker transport or real-money configuration is enabled by this checkpoint.

The next focused runtime branch should first pin the approved risk-policy version
and define shared risk-budget reservation across concurrent programs, then wire
the complete coordinator/accounting path without bypassing existing safety holds.
Complete request/plan persistence, reviewed migration and rollback, authenticated
recovery, source qualification and target-host tests remain part of that work.

Additional open scope remains in contract-level broker routing, cross-host fencing,
MCX admission and exact contract-to-fee/margin evidence, broader currency/segment
and settlement support, options/delivery behavior, approval-to-model deployment
binding, coordinated automatic backup and actual off-host restoration.

Qualified market data, authentic broker/contract-note/margin/settlement evidence,
forward after-cost strategy and calibration results, alert delivery, independent
security review and human acceptance are separate evidence requirements. Synthetic
tests do not qualify a strategy or prove that real execution losses are bounded.

The readiness/capability register must continue to distinguish engineering checks
from real-session and external evidence. Do not convert the engineering merge into
100% platform readiness, LIVE approval, or permission to start trading.
